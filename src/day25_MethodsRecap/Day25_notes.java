package day25_MethodsRecap;

public class Day25_notes {
            /*
            04/20/2020
        methods: void methods
        return method
        package: day25_MethodsRecap
        Warm up tasks:
        1. write a return method named getDriver1 that accepts a string
        parameter called Browser
        if the browser name matches with {"chrome",
        "firefox", "ie", "safari", "edge", "Opera"}, then it returns the name of
        that specific browser' driver
        Ex: getDriver("chrome");  ==> "Chrome
        Driver"
        getDriver("fireFOX"); ==>
        "FireFox Driver"
        ....
        if the browser name does not match with any
        of browsers above, the method should return "Invalid driver"
        APPLY SWITCH STATEMENTS
        2. do the first task with multi-branch if statement instead of
        switch statement
        3. do the first task with ternaries instead of switch statements
        method decleration:
        Access-modifers  Specifier  return-Type
        MethodName(paramter){
        statements;
        }
        Access-modifers: public, protected, default, private
        public: visible
        to the world
        Specifier: static, final, abstract, synchronbized
        static: can be called
        through class name
        return-types: void, any datatype
        void: does not return any
        value from the method
        datatype: return a value
        (MANDATORY)
        return statement: exits the current method
          can return value from a method,
        if return-type is not void
        System.exit(0): exits the entire system
        Tomorrow morning : method
          overloading
        Tomorrow afternoon: Git
        Wednesday' office hour: Methods Reveiw
        Thursday Morning: LocalDate & LocalDateTime
          DateTimeFormatter
        practicetask:
        1.  write a return method that accepts a String and removes
        duplicate values from the String
        Ex:
        RemoveDuplicate("aaabbbccc");  ==>
        "abc"
        2. write a return method that accepts two strings str1 & str2, and
        returns the frequency of str2 in str1 as an int
        Ex:
        Frequency("AAABB", "A");  ==> 3
        Frequency("ABAB", "B"); ==> 2
        3. (This one I will do it too tomorrow in class) Combine above two
        methods to write a return method that can return the frequency of
        characters as a string from any given string
        Ex:
        FrequencyOfChars("ABABCB"); ==> "A2B3C1";
        FrequencyOfChars("AAABCCCDD") ==> "A3B1C3D2"
        come back at: 4:10pm
        Canvas - > Java - > Modules - > Day25 -> Switch statements short quiz ->
        please completed
        please use chrome or fire fox browsers
        due by 4:30 pm
             */
}
