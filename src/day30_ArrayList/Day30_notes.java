package day30_ArrayList;

public class Day30_notes {
    /*

Topics: ArrayList Continue
package name: day30_ArrayList
WARMUP TASKS:
1. write a program that can return the sum of all the digits from a
string
ex:
input: "a1b2c3"
output: 6
(1+2+3= 6)
input: "Today's date is 04/27/2020"
output: 17
(0+4+2+7+2+0+2+0=17)
HINT: on ascii table, the characters between #48 ~ #57 are
digits
2. write a program that can print the list of integers in reversed
order
ex:
list=> {1,2,3,4,5}
output: 5 4 3 2 1
3. write a program that can find the unique elements from an
ArrayList of integers and stores into another list called uniques
ex: list==> {1,1,2,3,3,4,5}
uniques ==> {2,4,5}
4. write a program that can combine two String arrays into one
arrayList
ex:
arr1 = {"A", "B", "C"};
arr2 = {"D", "E", "F", "G"};
list ==> {"A", "B", "C", "D", "E", "F", "G"}
Methods:
add(Object): adds objects to the arraylist
add(index, Object): adds the object at the given index
list ==>   {1,3,4};
list.add(1 , 2)
list ==> {1,2,3,4}
list2 ==> {"A", "C", "D"}   "B"
list2.add("B"); ==> {A, C, D, B}
list2.add(1, "B");  ==> {A, B, C, D}
set(index, Object): replaces the original object at given index with
the new gievn object
replacing
remove(int index): object at the given index will be removed. ONLY one
remove(Object): given object will be removed, returns boolean. ONLY
one
clear(): remove everything from arraylist, size will be 0
indexOf(Object): returns the index number of the object, int
list ==> {"A", "A", "B"}
list.indexOf("A") ==> 0
     */
}
