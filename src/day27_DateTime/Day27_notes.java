package day27_DateTime;

public class Day27_notes {
            /*
            04/23/2020
        Topics: LocalDate
        Date & Time Formatting
        package name: day27_DateTime
        Warmup task:
        1. write a return method that can return the minimum
        number from an int array
        2. write a return method that can return the minimum
        number from a double array
        NOTE: apply method
        overloading,  DO NOT USE SORT METHOD.
        3. write a method that can print out the unique elements
        from an int array
        Ex: {1,1,2,3,3} ==> 2
        {6,6,7,7,8,9} ==>
        8 9
        4. write a method that can print out the unique elements
        from a double array
        Note: Apply method
        overloading
        LocalDate: used for creating date (year, months, days)
        default pattern: year-month-days
        LocalDate.of()
        LocalDate.now()
        methods:
        isAfter(): returns boolean
        isBefore(): returns boolean
        isEqual(): returns boolean
        isLeapyear(): returns boolean
        toString():
        format(DateTimeFormatter): returns String
        DateTimeFormatter:
         DateTimeFormatter dtf = DateTimeFormatter.ofPattern("")
         year: yy, yyyy
         Month: MM(number), MMM(three letters), MMMM(full name)
         days: dd
         days name: E(three letters), EEEE(full name)
        Canvas - > Java -> modules -> day27 -> Methods short quiz
        due by 12:35
        LocalTime: hours, minutes, second
     */
}
