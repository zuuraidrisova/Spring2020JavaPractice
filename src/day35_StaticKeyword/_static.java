package day35_StaticKeyword;

public class _static {

    /*
    Static keyword :

                => belong to class, can always be called thru the class name
                ==> class member
                ==>opposite of instance
                        @ instance belongs to object, every object has its own copy
                        @static belongs to class, there is only one copy  shared by all objects

              4 things can be static:

                   static variables
                   static methods
                   static blocks
                   static class
     */
}
