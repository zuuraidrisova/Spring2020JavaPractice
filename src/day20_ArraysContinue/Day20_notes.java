package day20_ArraysContinue;

public class Day20_notes {
            /*
            04/10/2020
        Topics: Arrays continue
        Arrays Utilities
        package name: day20_ArraysContinue
        class: Max_Min
        Warmup tasks:
        1. write a program that can find the maximum number from any given
        int array
        NOTE: DO NOT USE ANYTHING THAT WE HAVN'T COVERED IN THE
        CLASS
        2. write a program that can find the minimum number from any given
        int array
        NOTE: DO NOT USE ANYTHING THAT WE HAVN'T COVERED IN THE
        CLASS
        Array' size is fixed
        Arrays utilities:
        length: returns total number of the elemnt of the array
        Arrays Utilities:
        Arrays class:  presented in "java.util"
        import
        PackageName.className;
        import java.uti.Arrays;
        Arrays.toString(arrayName): converts the array to String,
        returns the string
        any array with any datatype
        Arrays.sort(arrayName): sorts the given array in
        ascending order
        int[] arr =
        {8,5,10,7, 12};
        Arrays.sort(arr);
        arr ==> {5,
        7, 8, 10, 12}
        Arrays.equals(arr1, arr2): chekcs if the two arrays are
        same or not. returns boolean
        int[] arr1 =
        {1,2,3};
        int[] arr2 =
        {1,2,3};
        Arrays.equal(arr1, arr2) ==> true
        String methods related to array:
        toCharArray(): returns char array from the string
        String str = "Batch 18";
        char[] ch =
        str.toCharArray(); //['B', 'a', 't', 'c', 'h', ' ', '1', '8']
        practice task:
        write a program that can combine two arrays of any lengths, and
        retunr the third array
        arr1 ==> [1,2,3]
        arr2 ==> [4,5,6,7];
        arr3 ==> [1,2,3,4,5,6,7]
        next week:
        1. Multidimentional Arrays
        2. for each loop
        3. Custom methods
             */
}
